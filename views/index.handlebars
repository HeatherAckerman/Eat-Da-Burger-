<h1>All the Burgers</h1>

<h2>Waiting to be devoured...</h2>

{{#each burgers}}
{{#unless devoured}}
{{this.id}} - {{this.burgerName}}
{{/unless}}
{{/each}}

<h2>Already devoured...</h2>
{{#each burgers}}
{{#if this.devoured}}
{{/if}}
{{/each}}

<h2>Add a Burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ca">What kind of burger do you want to devour?</label>
        <input type="text" id="kindOfBurger" name="name">
    </div>

    <button type="submit">Add your burger!</button>
</form>